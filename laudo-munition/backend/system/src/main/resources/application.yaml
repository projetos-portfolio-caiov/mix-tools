spring:
  application:
    name: backend

  datasource:
    url: jdbc:mysql://mysql:3306/db?useSSL=false&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=utf8&connectionCollation=utf8mb4_unicode_ci
    username: rootTESTE
    password: "senha_forte#101"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    defer-datasource-initialization: true
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: always

  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

server:
  address: 0.0.0.0

  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

  error:
    include-message: always
    include-binding-errors: always

jwt:
  validity: ${JWT_VALIDITY}
  secret: ${JWT_SECRET}